# 인증서 관리 시스템 - 기능 및 흐름 문서

## 📋 목차
1. [시스템 개요](#시스템-개요)
2. [주요 기능](#주요-기능)
3. [페이지 구조](#페이지-구조)
4. [데이터 흐름](#데이터-흐름)
5. [API 연동](#api-연동)
6. [사용자 흐름도](#사용자-흐름도)

---

## 🎯 시스템 개요

인증서 관리 시스템은 SSL/TLS 인증서를 관리하고 모니터링하는 웹 애플리케이션입니다. AI 채팅 어시스턴트를 통해 자연어로 인증서를 조회하고 필터링할 수 있습니다.

### 기술 스택
- **Frontend**: React + Vite
- **UI Library**: Radix UI, Lucide Icons
- **Styling**: CSS Modules
- **API**: REST API (Spring Boot Backend)
- **Deployment**: GitHub Pages

---

## ✨ 주요 기능

### 1. 인증서 목록 조회 및 표시
- **기능**: 백엔드 API에서 인증서 목록을 가져와 카드 형태로 표시
- **API**: `GET /api/v1/certificates`
- **특징**:
  - 페이지네이션 지원
  - 정렬 기능 (생성일 기준 내림차순)
  - 로딩 상태 표시
  - 에러 처리 및 재시도

### 2. 인증서 통계 대시보드
- **기능**: 인증서 상태별 통계 표시
- **표시 항목**:
  - 전체 인증서 수
  - 유효한 인증서 수
  - 곧 만료될 인증서 수 (30일 이내)
  - 만료된 인증서 수
- **위치**: 페이지 상단

### 3. 인증서 필터링 및 검색
- **필터링**:
  - 전체
  - 유효
  - 곧 만료
  - 만료됨
- **검색**:
  - 인증서 이름
  - 도메인
  - 인증서 유형
- **동작**: 실시간 필터링

### 4. 인증서 상세 조회
- **기능**: 특정 인증서의 상세 정보 표시
- **API**: `GET /api/v1/certificates/{id}`
- **표시 정보**:
  - 인증서 ID
  - 도메인
  - 유형
  - 발급 기관
  - 발급일
  - 만료일
  - 상태
  - 갱신 시도 횟수
  - 최근 오류 (있는 경우)
- **액션**: 삭제 버튼 제공

### 5. 인증서 생성
- **기능**: 새로운 인증서 발급 요청
- **API**: `POST /api/v1/certificates`
- **입력 필드**:
  - 도메인 (필수, 형식 검증)
  - 챌린지 타입 (선택: HTTP-01, DNS-01)
- **검증**:
  - 도메인 형식 검증 (정규식)
  - 필수 필드 검증
- **결과**: 생성된 인증서가 목록에 추가됨

### 6. 인증서 갱신
- **기능**: 만료 예정 또는 만료된 인증서 갱신
- **API**: `POST /api/v1/certificates/{id}/renew`
- **방법**:
  - 카드의 "갱신하기" 버튼 클릭
  - 상세 다이얼로그에서 갱신
  - AI 채팅에서 갱신 명령
- **결과**: 갱신된 인증서 정보 업데이트

### 7. 인증서 삭제
- **기능**: 인증서 삭제 (복구 불가)
- **API**: `DELETE /api/v1/certificates/{id}`
- **확인**: 삭제 전 확인 다이얼로그 표시
- **위치**: 상세 정보 다이얼로그

### 8. AI 채팅 어시스턴트
- **기능**: 자연어로 인증서 관리 수행
- **위치**: 우측 하단 플로팅 버튼 / 사이드바
- **주요 명령**:
  - 필터링: "유효한 인증서 보여줘", "만료된 인증서 필터링"
  - 조회: "인증서 목록", "인증서 상태", 특정 인증서 이름 검색
  - 갱신: "인증서 이름 갱신해줘"
  - 통계: "통계", "현황"
  - 도움말: "도움말", "help"

---

## 🏗️ 페이지 구조

```
App (메인 컴포넌트)
│
├── Header (헤더)
│   ├── 로고 및 제목
│   └── "새 인증서 추가" 버튼
│
├── Main Content (메인 콘텐츠)
│   ├── CertificateStats (통계 대시보드)
│   │   ├── 전체 인증서 수
│   │   ├── 유효한 인증서 수
│   │   ├── 곧 만료될 인증서 수
│   │   └── 만료된 인증서 수
│   │
│   ├── Filter Section (필터 및 검색)
│   │   ├── 검색 입력창
│   │   └── 상태 필터 드롭다운
│   │
│   └── Certificates Grid (인증서 카드 그리드)
│       └── CertificateCard (인증서 카드)
│           ├── 인증서 아이콘
│           ├── 인증서 정보
│           ├── 상태 배지
│           ├── 상세 정보
│           ├── "상세 보기" 버튼
│           └── "갱신하기" 버튼
│
├── Dialogs (다이얼로그)
│   ├── Renew Dialog (갱신 다이얼로그)
│   │   ├── 갱신 방법 선택
│   │   └── 알림 설정
│   │
│   ├── Add Dialog (추가 다이얼로그)
│   │   ├── 도메인 입력
│   │   └── 챌린지 타입 선택
│   │
│   └── Detail Dialog (상세 정보 다이얼로그)
│       ├── 인증서 상세 정보
│       ├── "삭제" 버튼
│       └── "닫기" 버튼
│
└── ChatSidebar (AI 채팅 사이드바)
    ├── 채팅 버튼 (플로팅)
    ├── 채팅 사이드바
    │   ├── 헤더
    │   ├── 메시지 영역
    │   └── 입력 영역
    └── AI 명령 처리 로직
```

---

## 🔄 데이터 흐름

### 1. 초기 로딩 흐름

```
사용자 접속
    ↓
App 컴포넌트 마운트
    ↓
useEffect 실행
    ↓
loadCertificates() 호출
    ↓
API: GET /api/v1/certificates
    ↓
응답 데이터 변환 (transformCertificateFromAPI)
    ↓
상태 업데이트 (setCertificates)
    ↓
화면 렌더링 (인증서 카드 표시)
```

### 2. 인증서 추가 흐름

```
"새 인증서 추가" 버튼 클릭
    ↓
Add Dialog 열기
    ↓
도메인 및 챌린지 타입 입력
    ↓
"추가하기" 버튼 클릭
    ↓
입력 검증 (도메인 형식, 필수 필드)
    ↓
API: POST /api/v1/certificates
    ↓
생성된 인증서 데이터 수신
    ↓
데이터 변환
    ↓
인증서 목록에 추가 (setCertificates)
    ↓
Dialog 닫기
    ↓
화면 업데이트 (새 인증서 카드 표시)
```

### 3. 인증서 갱신 흐름

```
"갱신하기" 버튼 클릭
    ↓
Renew Dialog 열기
    ↓
갱신 설정 선택 (옵션)
    ↓
"갱신 확인" 버튼 클릭
    ↓
API: POST /api/v1/certificates/{id}/renew
    ↓
갱신된 인증서 데이터 수신
    ↓
데이터 변환
    ↓
인증서 목록 업데이트 (해당 인증서 정보 갱신)
    ↓
Dialog 닫기
    ↓
화면 업데이트 (갱신된 정보 표시)
```

### 4. AI 채팅 필터링 흐름

```
사용자가 채팅에 "유효한 인증서 보여줘" 입력
    ↓
ChatSidebar의 handleSend() 실행
    ↓
generateMockResponse() 호출
    ↓
명령어 분석 (필터링 명령 감지)
    ↓
onFilterChange('valid') 호출
    ↓
App 컴포넌트의 setFilterStatus('valid')
    ↓
filteredCertificates 재계산
    ↓
화면 업데이트 (유효한 인증서만 표시)
    ↓
AI 응답 메시지 표시
```

### 5. 인증서 검색 및 필터 흐름

```
사용자가 검색어 입력 또는 필터 선택
    ↓
상태 업데이트 (setSearchQuery, setFilterStatus)
    ↓
filteredCertificates 재계산
    │
    ├── 검색 조건: 이름, 도메인, 유형 포함 여부
    └── 필터 조건: 상태 일치 여부
    ↓
화면 업데이트 (필터링된 인증서만 표시)
```

---

## 🔌 API 연동

### API 엔드포인트 목록

| 메서드 | 엔드포인트 | 설명 | 사용 위치 |
|--------|-----------|------|----------|
| GET | `/api/v1/certificates` | 인증서 목록 조회 | 초기 로딩, 목록 새로고침 |
| POST | `/api/v1/certificates` | 인증서 생성 | 인증서 추가 다이얼로그 |
| GET | `/api/v1/certificates/{id}` | 인증서 상세 조회 | 상세 보기 버튼, AI 채팅 |
| DELETE | `/api/v1/certificates/{id}` | 인증서 삭제 | 상세 다이얼로그 |
| POST | `/api/v1/certificates/{id}/renew` | 인증서 갱신 | 갱신 다이얼로그, AI 채팅 |
| GET | `/api/v1/health` | 헬스체크 | (준비됨, 미사용) |

### API 응답 형식

```javascript
// 성공 응답
{
  "success": true,
  "data": {
    // 실제 데이터
  },
  "message": "Success",
  "timestamp": "2024-01-01T00:00:00Z"
}

// 인증서 목록 응답
{
  "success": true,
  "data": {
    "content": [/* 인증서 배열 */],
    "page": 0,
    "size": 20,
    "totalElements": 100,
    "totalPages": 5
  }
}

// 인증서 응답
{
  "success": true,
  "data": {
    "id": 1,
    "domain": "example.com",
    "issuer": "Let's Encrypt",
    "issuedAt": "2024-01-01T00:00:00Z",
    "expiresAt": "2024-07-01T00:00:00Z",
    "status": "valid",
    "renewalAttempts": 0,
    "lastError": null
  }
}
```

### 에러 처리

- **네트워크 에러**: 에러 메시지 표시 및 재시도 버튼
- **API 에러**: 응답 메시지 표시
- **검증 에러**: 입력 필드 검증 실패 시 알림

---

## 👤 사용자 흐름도

### 시나리오 1: 인증서 목록 조회

```
1. 사용자가 페이지 접속
2. 인증서 목록 자동 로딩 (API 호출)
3. 통계 대시보드 표시
4. 인증서 카드 그리드 표시
5. (선택) 필터/검색으로 원하는 인증서 찾기
```

### 시나리오 2: 새 인증서 추가

```
1. "새 인증서 추가" 버튼 클릭
2. 다이얼로그 열림
3. 도메인 입력 (예: example.com)
4. (선택) 챌린지 타입 선택
5. "추가하기" 버튼 클릭
6. 입력 검증
7. API 호출 (인증서 생성 요청)
8. 성공 메시지 표시
9. 인증서 목록 자동 업데이트
10. 새로 생성된 인증서 카드 표시
```

### 시나리오 3: 인증서 갱신

```
1. 인증서 카드에서 "갱신하기" 버튼 클릭
2. 갱신 다이얼로그 열림
3. 갱신 방법 및 알림 설정 (선택)
4. "갱신 확인" 버튼 클릭
5. API 호출 (갱신 요청)
6. 성공 메시지 표시
7. 인증서 정보 업데이트
8. 화면 자동 새로고침
```

### 시나리오 4: AI 채팅으로 인증서 조회

```
1. 우측 하단 채팅 버튼 클릭
2. 사이드바 열림
3. "유효한 인증서 보여줘" 입력
4. AI가 명령 분석
5. 필터 자동 적용 (유효한 인증서만 표시)
6. AI 응답 메시지 표시
7. 화면에 필터링된 인증서 표시
```

### 시나리오 5: AI 채팅으로 인증서 갱신

```
1. 채팅 사이드바에서 "example.com 갱신해줘" 입력
2. AI가 인증서 찾기 (이름 또는 도메인 매칭)
3. API 호출 (갱신 요청)
4. 갱신 성공/실패 메시지 표시
5. 인증서 목록 자동 업데이트
```

---

## 🎨 UI 컴포넌트 구조

### CertificateCard
- **목적**: 개별 인증서 정보 표시
- **Props**:
  - `certificate`: 인증서 객체
  - `onRenew`: 갱신 콜백
  - `onViewDetails`: 상세 보기 콜백
- **표시 정보**: 이름, 도메인, 유형, 발급 기관, 발급일, 만료일, 남은 기간, 상태

### CertificateStats
- **목적**: 통계 대시보드 표시
- **Props**: `total`, `valid`, `expiringSoon`, `expired`
- **표시 형식**: 카드 형태의 통계

### ChatSidebar
- **목적**: AI 채팅 어시스턴트
- **Props**:
  - `onFilterChange`: 필터 변경 콜백
  - `stats`: 통계 정보
  - `certificates`: 인증서 목록
  - `onRenewCertificate`: 갱신 콜백
- **기능**: 자연어 명령 처리 및 필터링/조회/갱신

---

## 🔧 기술적 특징

### 상태 관리
- React Hooks 사용 (`useState`, `useEffect`)
- 로컬 상태 관리 (전역 상태 관리 라이브러리 미사용)

### 데이터 변환
- API 응답 형식 → 프론트엔드 형식 변환 함수
- 날짜 형식 변환 (ISO 8601 → YYYY-MM-DD)
- 상태 매핑 (API 상태 → 프론트엔드 상태)

### 에러 처리
- Try-catch 블록으로 에러 캡처
- 사용자 친화적 에러 메시지 표시
- 재시도 기능 제공

### 성능 최적화
- 필터링은 클라이언트 사이드에서 수행
- 필요한 경우에만 API 호출
- 로딩 상태 표시로 UX 개선

---

## 📝 추가 기능 제안

### 향후 개선 사항
1. **페이지네이션 UI**: 현재는 최대 100개만 가져옴, 페이지네이션 버튼 추가
2. **인증서 만료 알림**: 만료일 기준 자동 알림
3. **대량 작업**: 여러 인증서 선택하여 일괄 갱신/삭제
4. **내보내기**: 인증서 목록 CSV/Excel 내보내기
5. **히스토리**: 인증서 변경 이력 추적
6. **차트**: 인증서 상태별 시각화

---

## 🚀 배포

### 개발 환경
- Vite 개발 서버: `http://localhost:3000`
- 프록시 설정으로 CORS 문제 해결

### 프로덕션 환경
- GitHub Pages: `https://2025-Hanwha-AI-Hackathon.github.io/auto-cert-frontend/`
- GitHub Actions로 자동 빌드 및 배포

---

## 📚 참고 문서

- [CORS 해결 방법](./CORS_README.md)
- [API 명세서](https://auto-cert-backend-production.up.railway.app/swagger-ui/index.html)

