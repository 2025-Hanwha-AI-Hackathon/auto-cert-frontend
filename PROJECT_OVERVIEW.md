# 인증서 관리 시스템 - 프로젝트 개요

## 📋 프로젝트 개요

인증서 관리 시스템은 SSL/TLS 인증서를 효율적으로 관리할 수 있는 웹 애플리케이션입니다. AI 챗봇을 통한 직관적인 인터페이스와 백엔드 API 연동을 통해 인증서의 전체 생명주기를 관리할 수 있습니다.

## 🎯 주요 기능

### 1. 인증서 목록 관리
- **인증서 목록 조회**: 백엔드 API를 통한 실시간 인증서 목록 조회
- **페이지네이션**: 대량의 인증서를 효율적으로 관리
- **실시간 통계**: 전체, 유효, 곧 만료, 만료된 인증서 개수 표시

### 2. 인증서 필터링 & 검색
- **상태별 필터링**: 
  - 전체
  - 유효 (valid)
  - 곧 만료 (expiring-soon)
  - 만료됨 (expired)
- **검색 기능**: 인증서 이름, 도메인, 유형으로 검색
- **실시간 필터링**: 검색어와 필터 조건을 즉시 반영

### 3. 인증서 상세 관리
- **인증서 상세 조회**: 인증서 ID를 통한 상세 정보 조회
  - 인증서 ID, 도메인, 유형
  - 발급 기관, 발급일, 만료일
  - 상태, 갱신 시도 횟수
  - 최근 오류 메시지
- **인증서 추가**: 도메인과 챌린지 타입 입력으로 새 인증서 생성
- **인증서 갱신**: 만료 예정 인증서의 수동 갱신
- **인증서 삭제**: 인증서 삭제 (복구 불가)

### 4. AI 채팅 사이드바
- **자연어 기반 필터링**: 대화를 통해 인증서 필터링
- **인증서 상태 조회**: 텍스트로 인증서 정보 확인
- **인증서 갱신**: 대화를 통해 인증서 갱신
- **명령어 인식**:
  - 필터링: "유효한 인증서 보여줘", "만료된 인증서", "전체 보기"
  - 조회: "인증서 목록", "인증서 상태", "[인증서 이름]"
  - 갱신: "[인증서 이름] 갱신해줘"
  - 통계: "통계", "현황"
  - 도움말: "도움말", "help"

## 🔄 사용자 흐름 (User Flow)

### 1. 메인 화면 진입
```
사용자 접속
    ↓
인증서 목록 자동 로드 (API 호출)
    ↓
통계 카드 표시 (전체/유효/곧만료/만료됨)
    ↓
인증서 그리드 표시
```

### 2. 인증서 검색 & 필터링
```
검색어 입력 또는 필터 선택
    ↓
필터링된 인증서 목록 표시
    ↓
인증서 카드 표시
```

### 3. 인증서 상세 조회
```
인증서 카드에서 "상세 보기" 클릭
    ↓
API 호출: GET /api/v1/certificates/{id}
    ↓
상세 정보 다이얼로그 표시
    ↓
[선택] 갱신 또는 삭제
```

### 4. 인증서 추가
```
"새 인증서 추가" 버튼 클릭
    ↓
다이얼로그 열림
    ↓
도메인 입력 (필수) + 챌린지 타입 선택 (선택)
    ↓
API 호출: POST /api/v1/certificates
    ↓
성공 시 목록 자동 업데이트
```

### 5. 인증서 갱신
```
방법 1: 인증서 카드에서 "갱신하기" 버튼 클릭
    ↓
갱신 다이얼로그 표시
    ↓
갱신 확인
    ↓
API 호출: POST /api/v1/certificates/{id}/renew
    ↓
성공 시 목록 업데이트

방법 2: AI 채팅에서 "인증서 이름 갱신해줘"
    ↓
AI가 인증서 찾기
    ↓
API 호출: POST /api/v1/certificates/{id}/renew
    ↓
갱신 결과 텍스트로 응답
```

### 6. AI 채팅 사용
```
채팅 버튼 클릭 (우측 하단)
    ↓
사이드바 열림
    ↓
사용자 메시지 입력
    ↓
AI 명령 인식
    ↓
적절한 액션 실행 (필터링/조회/갱신)
    ↓
텍스트 응답 표시
```

## 🏗️ 시스템 아키텍처

```
┌─────────────────────────────────────────────────────────────┐
│                    Frontend (React + Vite)                  │
│                                                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │   App.jsx    │  │ ChatSidebar  │  │  Components  │     │
│  │              │  │     .jsx     │  │              │     │
│  │ - 상태 관리   │  │ - AI 챗봇    │  │ - Certificate│     │
│  │ - API 호출   │  │ - 명령 인식   │  │   Card       │     │
│  │ - 필터링     │  │ - 필터링     │  │ - Stats      │     │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘     │
│         │                  │                  │              │
│         └──────────────────┼──────────────────┘             │
│                            │                                 │
│                  ┌─────────▼─────────┐                      │
│                  │   services/api.js │                      │
│                  │                   │                      │
│                  │ - API 함수 모음   │                      │
│                  │ - 응답 처리       │                      │
│                  │ - 에러 핸들링     │                      │
│                  └─────────┬─────────┘                      │
└────────────────────────────┼─────────────────────────────────┘
                             │
                    ┌────────▼────────┐
                    │  Vite Proxy     │ (개발 환경)
                    │  /api/*         │
                    └────────┬────────┘
                             │
┌────────────────────────────▼─────────────────────────────────┐
│           Backend API (Spring Boot)                          │
│   https://auto-cert-backend-production.up.railway.app        │
│                                                               │
│  GET    /api/v1/certificates          인증서 목록 조회       │
│  POST   /api/v1/certificates          인증서 생성            │
│  GET    /api/v1/certificates/{id}     인증서 상세 조회       │
│  DELETE /api/v1/certificates/{id}     인증서 삭제            │
│  POST   /api/v1/certificates/{id}/renew 인증서 갱신          │
│  GET    /api/v1/health                헬스체크               │
└───────────────────────────────────────────────────────────────┘
```

## 📱 화면 구성

### 메인 화면
```
┌────────────────────────────────────────────────────────┐
│  Header                                                │
│  [🛡️ 인증서 관리 시스템]           [+ 새 인증서 추가]  │
├────────────────────────────────────────────────────────┤
│  통계 카드                                              │
│  ┌──────┐  ┌──────┐  ┌──────┐  ┌──────┐              │
│  │ 전체 │  │ 유효 │  │곧만료│  │만료됨│              │
│  │  10  │  │  7   │  │  2   │  │  1   │              │
│  └──────┘  └──────┘  └──────┘  └──────┘              │
├────────────────────────────────────────────────────────┤
│  필터 & 검색                                            │
│  [🔍 검색창...]                  [▼ 필터: 전체]        │
├────────────────────────────────────────────────────────┤
│  인증서 그리드                                           │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐            │
│  │ 인증서 1  │  │ 인증서 2  │  │ 인증서 3  │            │
│  │ 도메인    │  │ 도메인    │  │ 도메인    │            │
│  │ 발급기관  │  │ 발급기관  │  │ 발급기관  │            │
│  │ 만료일    │  │ 만료일    │  │ 만료일    │            │
│  │ [상세]    │  │ [상세]    │  │ [상세]    │            │
│  │ [갱신]    │  │ [갱신]    │  │ [갱신]    │            │
│  └──────────┘  └──────────┘  └──────────┘            │
│                                                         │
└────────────────────────────────────────────────────────┘
                                                    [💬 채팅 버튼]
```

### AI 채팅 사이드바
```
┌────────────────────────────────────┐
│  ChatGPT Assistant            [X]  │
├────────────────────────────────────┤
│  💬 안녕하세요! 인증서 관리에 대해   │
│     도움이 필요하신가요?            │
│                                     │
│  👤 유효한 인증서 보여줘            │
│                                     │
│  🤖 ✅ 유효한 인증서만 보여드리겠  │
│     습니다. 총 7개의 유효한 인증서  │
│     가 있습니다.                    │
│                                     │
│  👤 example.com 갱신해줘           │
│                                     │
│  🤖 ✅ "example.com" 인증서가 성공 │
│     적으로 갱신되었습니다!          │
│                                     │
├────────────────────────────────────┤
│  [메시지 입력...]              [📤] │
└────────────────────────────────────┘
```

## 🔌 API 연동 상세

### API 엔드포인트

| Method | Endpoint | 기능 | 요청/응답 |
|--------|----------|------|-----------|
| GET | `/api/v1/certificates` | 인증서 목록 조회 | Query: page, size, sort |
| POST | `/api/v1/certificates` | 인증서 생성 | Body: {domain, challengeType?} |
| GET | `/api/v1/certificates/{id}` | 인증서 상세 조회 | Path: id |
| DELETE | `/api/v1/certificates/{id}` | 인증서 삭제 | Path: id (204 No Content) |
| POST | `/api/v1/certificates/{id}/renew` | 인증서 갱신 | Path: id |
| GET | `/api/v1/health` | 헬스체크 | - |

### 응답 형식
```json
{
  "success": true,
  "data": {
    // 실제 데이터
  },
  "message": "성공 메시지",
  "errorCode": null,
  "timestamp": "2024-01-01T00:00:00"
}
```

### 데이터 변환
- **API → Frontend**: ISO 8601 날짜 → YYYY-MM-DD
- **Status 매핑**: valid, expiring-soon, expired
- **도메인 기반 이름 생성**: 도메인이 없으면 "인증서 #ID" 사용

## 🎨 주요 컴포넌트

### 1. App.jsx
- **역할**: 메인 애플리케이션 컴포넌트
- **상태 관리**: 인증서 목록, 필터, 검색어, 다이얼로그 상태
- **주요 함수**:
  - `loadCertificates()`: API에서 인증서 목록 가져오기
  - `handleAddCertificate()`: 새 인증서 추가
  - `handleRenew()`: 인증서 갱신
  - `handleDeleteCertificate()`: 인증서 삭제
  - `handleViewDetails()`: 인증서 상세 조회

### 2. ChatSidebar.jsx
- **역할**: AI 챗봇 사이드바
- **주요 기능**:
  - 명령어 인식 및 파싱
  - 필터링 트리거
  - 인증서 검색 및 갱신
  - 텍스트 응답 생성

### 3. CertificateCard.jsx
- **역할**: 인증서 카드 UI
- **표시 정보**: 이름, 도메인, 발급 기관, 만료일, 상태, 남은 기간

### 4. CertificateStats.jsx
- **역할**: 통계 카드 UI
- **표시 정보**: 전체/유효/곧만료/만료됨 개수

## 🚀 배포 환경

### 개발 환경
- **로컬**: `http://localhost:3000`
- **프록시**: Vite 프록시 사용 (`/api/*` → 백엔드)
- **CORS**: 프록시를 통한 우회

### 프로덕션 환경
- **GitHub Pages**: `https://2025-Hanwha-AI-Hackathon.github.io/auto-cert-frontend/`
- **CI/CD**: GitHub Actions 자동 배포
- **API**: 직접 백엔드 URL 사용
- **CORS**: 백엔드에서 허용 필요

## 🔐 보안 및 에러 처리

### 에러 처리
- API 응답 상태 코드 확인
- 네트워크 에러 처리
- 사용자 친화적 에러 메시지
- 로딩 상태 표시

### CORS 처리
- 개발 환경: Vite 프록시 사용
- 프로덕션 환경: 백엔드 CORS 설정 필요

## 📊 상태 관리

### 인증서 상태
- `valid`: 유효한 인증서
- `expiring-soon`: 곧 만료될 인증서
- `expired`: 만료된 인증서

### 필터 상태
- `all`: 전체
- `valid`: 유효
- `expiring-soon`: 곧 만료
- `expired`: 만료됨

## 🎯 주요 특징

1. **실시간 데이터**: API를 통한 실시간 인증서 정보
2. **AI 기반 인터페이스**: 자연어로 인증서 관리
3. **직관적인 UI**: 카드 기반 레이아웃
4. **반응형 디자인**: 모바일/태블릿/데스크톱 지원
5. **효율적인 필터링**: 검색 + 상태 필터 동시 사용

## 📝 향후 개선 사항

- [ ] 페이지네이션 UI 추가
- [ ] 인증서 자동 갱신 스케줄링
- [ ] 알림 기능 (만료 전 알림)
- [ ] 인증서 내보내기/가져오기
- [ ] 통계 차트 및 그래프
- [ ] 다중 도메인 인증서 생성
- [ ] 인증서 이력 관리

